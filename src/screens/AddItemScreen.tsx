import React from 'react';
import {View, StyleSheet,Text} from 'react-native';

const AddItemScreen = (props: any) => {
    return (
        <View>
            <Text>Add item screenio</Text>
        </View>
    );

}

const styles = StyleSheet.create({
    
})

export default AddItemScreen;